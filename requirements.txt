# StickyNote 核心依赖
PyQt5>=5.15
pywin32>=305

# 加密（可选：主密码保护）
cryptography>=41.0
argon2-cffi>=23.1

# Markdown 渲染（可选）
markdown>=3.5
Pygments>=2.17

# 云同步（可选）
requests>=2.31
webdavclient3>=3.14

# 开发依赖
pytest>=8.0
pytest-qt>=4.4
pytest-cov>=5.0
