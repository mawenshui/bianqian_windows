# 历史文档归档

本目录只保存已被当前 `docs/` 文档集替代的历史资料，用于追溯设计演进。

- `legacy-root/`：原先散落在仓库根目录的旧版规范、改进计划和优化计划。
- `legacy-docs/`：原先位于 `docs/`、但已被编号文档集替代的旧版综合文档。

归档内容中的版本号、路径和实现细节可能已经过时，不作为当前开发或发布依据。当前规范以根目录 `AGENTS.md` 和 `docs/README.md` 索引的文档为准。
